<div class="container">
    <div class="row">
    <div class="span5 offset4 well">
    <legend align="center"><h2>Acceder al Sistema</h2></legend>
  
<%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
  <div><%= f.label :nombre, "Nombre Usuario:" %><br />
  <%= f.email_field :nombre, autofocus: true, :style=>'width: 400px;' %></div>

  <div><%= f.label :password, "Contraseña:"%><br />
    <%= f.password_field :password, autocomplete: "off", :style=>'width: 400px;' %></div>

  <% if devise_mapping.rememberable? -%>
    <div><%= f.check_box :remember_me%> <%= f.label :remember_me, "Recordarme" %></div>
  <% end -%> <br /><br /><br />

  <div align="center"><%= f.submit "Aceptar", :style=>'width: 200px;'%></div>
<% end %>

<%= render "devise/shared/links" %>
</div>
  </div>
</div>